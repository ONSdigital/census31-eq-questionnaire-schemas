---
# Configuration file for MegaLinter
#
# See all available variables at https://megalinter.io/latest/config-file/ and in
# linters documentation
# all, none, or list of linter keys

# If you use ENABLE variable, all other languages/formats/tooling-formats will
# be disabled by default
# Have decided not to enable the Python linter as has been recommended in point 7
# in the [python template readme](https://github.com/ONSdigital/ons-python-template#3-megalinter)
ENABLE:
  - ACTION
  - BASH
  - EDITORCONFIG
  - JSON
  - MARKDOWN
  - REPOSITORY
  - YAML

FORMATTERS_DISABLE_ERRORS: false # Ensures code is actually fixable when using `make megalint-apply`
SHOW_ELAPSED_TIME: true # Displays execution time for each linter
FLAVOR_SUGGESTIONS: false # Disables suggestions to use different MegaLinter flavors

DISABLE_LINTERS:
  - REPOSITORY_GRYPE # No container images in this repo
  - REPOSITORY_CHECKOV # Vulns covered by osv-scanner; IaC too limited here to justify 17s
  - REPOSITORY_TRIVY # osv-scanner covers vulnerabilities faster (1.82s vs 18.68s)
  - REPOSITORY_TRIVY_SBOM # Redundant with trivy (also disabled)
  - REPOSITORY_TRUFFLEHOG # Running on changed files only; secretlint is sufficient
  - REPOSITORY_GITLEAKS # Overlaps with secretlint; 12s vs 2s
  - REPOSITORY_KINGFISHER # Overlaps with secretlint; 10s vs 2s
  - REPOSITORY_BETTERLEAKS # Overlaps with secretlint
  - REPOSITORY_SYFT # Generates an SBOM artifact but never fails the lint check

ACTION_ZIZMOR_UNSECURED_ENV_VARIABLES:
  - GITHUB_TOKEN
